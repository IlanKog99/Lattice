"""Lattice — a terminal grid manager.

A small TUI for keeping a grid of labelled cells. The on-disk store is
machine-encoded (not human readable) and is only ever expanded into plain
text inside memory while the app is running.
"""

__version__ = "1.1.1"
APP_NAME = "Lattice"
