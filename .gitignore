# The data store — never commit it. It holds everything, encoded.
grid.dat
grid.tmp
*.dat
*.tmp

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# Local-only release publisher — personal machine, never shipped
release.ps1

# Marks the last version the What's New popup has been shown for
.whatsnew_seen

# Local AI-tooling state — plugin configs, hook caches, agent scratch dirs.
# Never contains project code, just this machine's tool runtime state.
.claude/
.impeccable/
.superpowers/
docs/superpowers/
